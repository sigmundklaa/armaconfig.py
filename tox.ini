[tox]
passenv = CI TRAVIS TRAVIS_*
envlist = py37,py38, coverage

[testenv]
deps = pytest
changedir = tests
commands = pytest

[testenv:coverage]
deps =
    pytest
    codecov
commands =
    coverage erase
    coverage run -m pytest
    codecov
