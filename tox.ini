[tox]
envlist = python3.7, python3.8, coverage

[testenv]
deps = pytest
changedir = tests
commands = pytest

[testenv:coverage]
passenv = CI TRAVIS TRAVIS_*
deps =
    pytest
    codecov
commands =
    coverage erase
    coverage run -m pytest
    codecov
